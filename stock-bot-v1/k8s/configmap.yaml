apiVersion: v1
kind: ConfigMap
metadata:
  name: stock-bot-config
  namespace: stock-bot
data:
  # Application configuration
  PYTHONUNBUFFERED: "1"
  STREAMLIT_SERVER_PORT: "8501"
  FLASK_PORT: "5001"
  # AWS Configuration (will be overridden by secrets)
  AWS_DEFAULT_REGION: "us-west-2"
