apiVersion: v1
kind: PersistentVolume
metadata:
  name: jupyter-fixed-pv
  namespace: stock-bot
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: hostpath
  hostPath:
    path: /tmp/jupyter-data

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jupyter-fixed-pvc
  namespace: stock-bot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: hostpath

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jupyter-fixed
  namespace: stock-bot
  labels:
    app: jupyter-fixed
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jupyter-fixed
  template:
    metadata:
      labels:
        app: jupyter-fixed
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 100
        fsGroup: 100
      containers:
      - name: jupyter
        image: jupyter/minimal-notebook:latest
        ports:
        - containerPort: 8888
        env:
        - name: JUPYTER_ENABLE_LAB
          value: "yes"
        - name: JUPYTER_TOKEN
          value: "stock-bot-jupyter-2024"
        - name: PYTHONPATH
          value: "/home/jovyan/work:/home/jovyan/work/src"
        resources:
          requests:
            memory: "1Gi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "1000m"
        volumeMounts:
        - name: persistent-storage
          mountPath: /home/jovyan/work
        command: ["jupyter"]
        args: ["lab", "--ip=0.0.0.0", "--port=8888", "--no-browser", "--allow-root", "--NotebookApp.token=stock-bot-jupyter-2024", "--NotebookApp.password=", "--NotebookApp.disable_check_xsrf=True"]
      volumes:
      - name: persistent-storage
        persistentVolumeClaim:
          claimName: jupyter-fixed-pvc
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: jupyter-fixed-service
  namespace: stock-bot
spec:
  selector:
    app: jupyter-fixed
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8888
  type: LoadBalancer
